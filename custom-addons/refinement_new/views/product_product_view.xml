<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="product_refinement_inherit_form_view" model="ir.ui.view">
      <field name="name">product.refinement.form</field>
      <field name="model">product.template</field>
      <field name="inherit_id" ref="product.product_template_form_view"/>
      <field name="arch" type="xml">
        <div name="options" position="inside">
          <div>
            <field name="is_template"/>
            <label for="is_template"/>
          </div>
        </div>
        <xpath expr="//group[@name='group_general']/field[@name='type']" position="before">
            <field name="is_refinery" groups="refinement_new.group_refinement_user,refinement_new.group_refinement_manager" invisible="1"/>
        </xpath>
        <!-- <xpath expr="//page[@name='sales']" position="after">
          <page name="refinement" string="Refinement" groups="refinement_new.group_refinement_user, refinement_new.group_refinement_manager">
            <group>
              <separator string="Handling Group" colspan="4"/>
              <field name="handling_group_id" nolabel="1" colspan="4"/>
              <separator string="Branding Items" colspan="4"/>
              <field name="branding_ids" widget="many2many_tags" nolabel="1" colspan="4"/>
            </group>
          </page>
        </xpath> -->
        <field name="list_price" position="before">
            <field name="brand"/>
            <field name="cat_page"/>
            <field name="material"/>
            <field name="chara"/>
            <field name="dimension"/>
            <field name="cust_terif_num"/>
            <field name="mfg_country"/>
            <field name="self_life"/>
            <field name="brand_inf"/>
        </field>
      </field>
    </record>
</odoo>